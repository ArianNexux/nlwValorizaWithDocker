version: '3'

services:
    api: 
      build: ./backend
      ports:
        - 3000:3000
      volumes:
        - ./backend:/app
    dbname: 
      image: mysql:latest
      ports: 
        - 3306:3306   
      volumes:
        - ./database:/var/lib/mysql

